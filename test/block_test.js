var expect = require('chai').expect;
var fs = require('fs');
var moChunks = require(__dirname + '/../mochunks.js');

describe('the block constructor', function() {
  before('run the program', function() {
    moChunks();
  })

  it('should create an object with certain properties');
});
